info:
  name: List Access Policies
  type: http
  seq: 7

http:
  method: GET
  url: "{{ENDPOINT}}/v1/cwobject/access-policy"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
