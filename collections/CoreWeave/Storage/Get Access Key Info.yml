info:
  name: Get Access Key Info
  type: http
  seq: 6

http:
  method: GET
  url: "{{ENDPOINT}}/v1/cwobject/access-key/{{ACCESS_KEY_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
