info:
  name: Create Access Key
  type: http
  seq: 5

http:
  method: POST
  url: "{{ENDPOINT}}/v1/cwobject/access-key"
  body:
    type: json
    data: |-
      {
        "durationSeconds": 3600
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
