info:
  name: List Bucket Info
  type: http
  seq: 1

http:
  method: GET
  url: "{{ENDPOINT}}/v1/cwobject/bucket-info"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
