info:
  name: Get Bucket Info
  type: http
  seq: 2

http:
  method: GET
  url: "{{ENDPOINT}}/v1/cwobject/bucket-info/{{BUCKET_NAME}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
