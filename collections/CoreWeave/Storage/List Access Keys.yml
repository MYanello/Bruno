info:
  name: List Access Keys
  type: http
  seq: 4

http:
  method: GET
  url: "{{ENDPOINT}}/v1/cwobject/access-key"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
