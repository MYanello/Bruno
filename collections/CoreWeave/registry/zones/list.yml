info:
  name: List Zones
  type: http
  seq: 1

http:
  method: GET
  url: "{{ENDPOINT}}/v1alpha1/registry/zones"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
