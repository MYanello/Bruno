info:
  name: List Registry Quota Statuses
  type: http
  seq: 1

http:
  method: GET
  url: "{{ENDPOINT}}/v1alpha1/registry/quotaStatuses"
  auth: inherit

headers:
  X-Remote-OrgUid: "{{ORG_UID}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
