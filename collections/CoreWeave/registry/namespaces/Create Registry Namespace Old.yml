info:
  name: Create Registry Namespace Old
  type: http
  seq: 6

http:
  method: POST
  url: "{{ENDPOINT}}/v1alpha1/registry/namespaces/{{NAMESPACE}}"
  body:
    type: json
    data: |-
      {
        "zone": "{{ZONE}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
