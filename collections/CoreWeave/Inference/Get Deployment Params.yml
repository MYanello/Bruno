info:
  name: Get Deployment Params
  type: http
  seq: 4

http:
  method: GET
  url: "{{ENDPOINT}}/v1alpha1/inference/deployments/parameters"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
