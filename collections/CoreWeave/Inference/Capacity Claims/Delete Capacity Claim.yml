info:
  name: Delete Capacity Claim
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{ENDPOINT}}/v1alpha1/inference/capacityclaims/{{CAPACITY_CLAIM_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
