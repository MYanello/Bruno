info:
  name: Get Capacity Claim
  type: http
  seq: 3

http:
  method: GET
  url: "{{ENDPOINT}}/v1alpha1/inference/capacityclaims/{{CAPACITY_CLAIM_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
