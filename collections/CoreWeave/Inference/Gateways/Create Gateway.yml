info:
  name: Create Gateway
  type: http
  seq: 4

http:
  method: POST
  url: "{{ENDPOINT}}/v1alpha1/inference/gateways"
  body:
    type: json
    data: |-
      {
        "name": "myanello",
        "zones": ["RNO2A"],
        "coreWeaveAuth": {},
        "bodyBasedRouting": {
          "apiType": "API_TYPE_OPENAI"
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
