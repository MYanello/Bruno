info:
  name: Get Gateway
  type: http
  seq: 3

http:
  method: GET
  url: "{{ENDPOINT}}/v1alpha1/inference/gateways/{{GATEWAY_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
