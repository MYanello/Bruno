info:
  name: List VPCs
  type: http
  seq: 1

http:
  method: GET
  url: "{{ENDPOINT}}/v1beta1/networking/vpcs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
