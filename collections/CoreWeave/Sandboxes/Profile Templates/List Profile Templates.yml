info:
  name: List Profile Templates
  type: http
  seq: 1

http:
  method: GET
  url: "{{ENDPOINT}}/v1beta2/sandbox/profile-templates"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
