info:
  name: Delete Cluster
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{ENDPOINT}}/v1beta1/cks/clusters/{{CLUSTER_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
