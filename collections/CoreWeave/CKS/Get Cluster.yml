info:
  name: Get Cluster
  type: http
  seq: 3

http:
  method: GET
  url: "{{ENDPOINT}}/v1beta1/cks/clusters/{{CLUSTER_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
