info:
  name: Get Flow
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/v3/flows/instances/{{slug}}/"
  auth: inherit

runtime:
  variables:
    - name: slug
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
