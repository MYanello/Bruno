info:
  name: Get Users
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/v3/core/users/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
