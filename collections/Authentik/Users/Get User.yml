info:
  name: Get User
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}/api/v3/core/users/{{pk}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
