info:
  name: Delete User
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{host}}/api/v3/core/users/{{pk}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
