info:
  name: Delete Group Connection
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{host}}/sources/group_connections/oauth/{{pk}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
