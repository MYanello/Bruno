info:
  name: Options
  type: http
  seq: 5

http:
  method: OPTIONS
  url: "{{host}}/api/v3/sources/oauth/{{source}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
