info:
  name: Get Source
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/api/v3/sources/oauth/{{source}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
