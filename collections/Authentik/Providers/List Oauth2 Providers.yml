info:
  name: List Oauth2 Providers
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/api/v3/providers/oauth2/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
